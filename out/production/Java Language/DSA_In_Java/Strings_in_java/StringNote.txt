IMP:- 
1) Comparator => (a, b) -> b - a;
class myClass implements Comparator<String>{
    public int compare(String s1, String s2) {
        return s2 - s1;
    }
}

Tips :- Comparator<student> comparator = comparator.comparing(student :: getName).reverse().thenComparing(student :: Age);
      